# Demo mode will not connect to IMAP, call a real AI API, or send email.
DEMO_MODE=true

COMPANY_NAME=示例科技
DIGEST_TO=manager@example.com,ops@example.com
DIGEST_CC=
TIMEZONE=Asia/Shanghai
DIGEST_PREVIEW_DIR=./preview

# These values are placeholders in demo mode.
IMAP_HOST=imap.example.com
IMAP_USER=demo@example.com
IMAP_PASSWORD=demo
SMTP_HOST=smtp.example.com
SMTP_USER=demo@example.com
SMTP_PASSWORD=demo
MAIL_FROM=demo@example.com
LLM_API_KEY=demo
